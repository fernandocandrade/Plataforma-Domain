class Persistence:

    def create(self, objects):
        """ creates new objects from list """
        print(f"creating {objects}")

    def update(self, objects):
        print(f"updating {objects}")

    def destroy(self, objects):
        print(f"destroying {objects}")

