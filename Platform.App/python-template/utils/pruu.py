from utils.http import HttpClient

_client = HttpClient()

def log_on_pruu(key, data):
    """try:
        _client.post(f"https://pruu.herokuapp.com/dump/{key}", data)
    except:
        pass"""
    pass
